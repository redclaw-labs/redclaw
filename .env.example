# RedClaw Environment Variables
# Copy this file to .env and fill in your values.
# NEVER commit .env — it is listed in .gitignore.

# ── Required ──────────────────────────────────────────────────
# Your LLM provider API key
# REDCLAW_API_KEY=sk-your-key-here
API_KEY=your-api-key-here

# ── Provider & Model ─────────────────────────────────────────
# LLM provider: openrouter, openai, anthropic, ollama, glm
PROVIDER=openrouter
# REDCLAW_MODEL=anthropic/claude-sonnet-4-20250514
# REDCLAW_TEMPERATURE=0.7

# ── Gateway ──────────────────────────────────────────────────
# REDCLAW_GATEWAY_PORT=3000
# REDCLAW_GATEWAY_HOST=127.0.0.1
# REDCLAW_ALLOW_PUBLIC_BIND=false

# ── Workspace ────────────────────────────────────────────────
# REDCLAW_WORKSPACE=/path/to/workspace

# ── Docker Compose ───────────────────────────────────────────
# Host port mapping (used by docker-compose.yml)
# HOST_PORT=3000
