# RedClaw Environment Variables
# Copy this file to .env and fill in your values.
# NEVER commit .env — it is listed in .gitignore.

# ── Required ──────────────────────────────────────────────────
# Your LLM provider API key
# REDCLAW_API_KEY=sk-your-key-here
API_KEY=your-api-key-here

# ── Provider & Model ─────────────────────────────────────────
# LLM provider: openrouter, openai, anthropic, ollama, glm
PROVIDER=openrouter
# REDCLAW_MODEL=anthropic/claude-sonnet-4-6
# REDCLAW_TEMPERATURE=0.7

# ── Provider API Keys (optional) ─────────────────────────────
# MINIMAX_OAUTH_TOKEN=...
# MINIMAX_API_KEY=...
# MINIMAX_OAUTH_REFRESH_TOKEN=...
# MINIMAX_OAUTH_REGION=global   # optional: global|cn

# ── Gateway ──────────────────────────────────────────────────
# REDCLAW_GATEWAY_PORT=3000
# REDCLAW_GATEWAY_HOST=127.0.0.1
# REDCLAW_ALLOW_PUBLIC_BIND=false

# ── Storage ─────────────────────────────────────────────────
# Backend override for persistent storage (default: sqlite)
# REDCLAW_STORAGE_PROVIDER=sqlite
# REDCLAW_STORAGE_DB_URL=postgres://localhost/redclaw
# REDCLAW_STORAGE_CONNECT_TIMEOUT_SECS=5

# ── Workspace ────────────────────────────────────────────────
# REDCLAW_WORKSPACE=/path/to/workspace

# Reasoning mode (enables extended thinking for supported models)
# REDCLAW_REASONING_ENABLED=false
# REASONING_ENABLED=false

# ── Proxy ──────────────────────────────────────────────────
# Forward provider/service traffic through an HTTP(S) proxy.
# REDCLAW_PROXY_ENABLED=false
# REDCLAW_HTTP_PROXY=http://proxy.example.com:8080
# REDCLAW_HTTPS_PROXY=http://proxy.example.com:8080
# REDCLAW_ALL_PROXY=socks5://proxy.example.com:1080
# REDCLAW_NO_PROXY=localhost,127.0.0.1
# REDCLAW_PROXY_SCOPE=redclaw          # environment|redclaw|services
# REDCLAW_PROXY_SERVICES=openai,anthropic

# ── Docker Compose ───────────────────────────────────────────
# Host port mapping (used by docker-compose.yml)
# HOST_PORT=3000

# ── Z.AI GLM Coding Plan ───────────────────────────────────────
# Z.AI provides GLM models through OpenAI-compatible endpoints.
# API key format: id.secret (e.g., abc123.xyz789)
#
# Usage:
#   redclaw onboard --provider zai --api-key YOUR_ZAI_API_KEY
#
# Or set the environment variable:
# ZAI_API_KEY=your-id.secret
#
# Common models: glm-5, glm-4.7, glm-4-plus, glm-4-flash
# See docs/zai-glm-setup.md for detailed configuration.

# ── Web Search ────────────────────────────────────────────────
# Web search tool for finding information on the internet.
# Enabled by default with DuckDuckGo (free, no API key required).
#
# WEB_SEARCH_ENABLED=true
# WEB_SEARCH_PROVIDER=duckduckgo
# WEB_SEARCH_MAX_RESULTS=5
# WEB_SEARCH_TIMEOUT_SECS=15
#
# Optional: Brave Search (requires API key from https://brave.com/search/api)
# WEB_SEARCH_PROVIDER=brave
# BRAVE_API_KEY=your-brave-search-api-key
